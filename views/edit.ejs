<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-between items-center w-full">
            <a class="text-blue-500 mb-10" href="/profile">Go back</a>
            <a class="bg-red-500 rounded-md px-3 py-2 inline-block mb-10" href="/logout">Logout</a>
        </div>
        <h5 class="mb-3 text-3xl">Edit your post</h5>
        <form action="/update/<%=post._id%>" method="post">
           <textarea class="w-1/2 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md block" placeholder="What's in your mind?" name="content"><%= post.content %></textarea>
           <input class="px-3 w-40 py-3 bg-yellow-500 text-black block rounded-md mt-3 text-sm" type="submit" value="Update Post">
        </form>


        

        
    </div>
</body>
</html>